spring:
  application:
    name: driver-service

  datasource:
    url: jdbc:postgresql://localhost:5432/driver_db?createDatabaseIfNotExist=true
    username: postgres
    password: dsova2207_12
    driver-class-name: org.postgresql.Driver

  jpa:
    show:
      sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


server:
  post: 8080
